<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/s2</item>
        <item name="colorPrimaryDark">@color/s2</item>
        <item name="colorAccent">#FF4081</item>
        <item name="android:windowBackground">@color/s2</item>
        <item name="android:colorBackground">@color/s2</item>
        <item name="android:indeterminateDuration">1500</item>
    </style>

</resources>
